import {
  ActionIcon,
  Box,
  Button,
  Center,
  Checkbox,
  Collapse,
  Group,
  Loader,
  Menu,
  Pagination,
  Paper,
  Popover,
  Portal,
  ScrollArea,
  Table,
  Text,
  UnstyledButton,
  createStyles,
  packSx,
  px,
  useMantineTheme
} from "./chunk-YZZZF4RF.js";
import "./chunk-3QTN4EH6.js";
import "./chunk-JUSISOPU.js";
import {
  useClickOutside,
  useDisclosure,
  useElementSize,
  useMediaQuery,
  useMergedRef,
  useTimeout,
  useWindowEvent
} from "./chunk-OLTNSOUR.js";
import {
  require_react
} from "./chunk-AXJ2GJWQ.js";
import {
  __commonJS,
  __toESM
} from "./chunk-TCQZMY3T.js";

// node_modules/react/cjs/react-jsx-runtime.development.js
var require_react_jsx_runtime_development = __commonJS({
  "node_modules/react/cjs/react-jsx-runtime.development.js"(exports) {
    "use strict";
    if (true) {
      (function() {
        "use strict";
        var React = require_react();
        var REACT_ELEMENT_TYPE = Symbol.for("react.element");
        var REACT_PORTAL_TYPE = Symbol.for("react.portal");
        var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
        var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
        var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
        var REACT_PROVIDER_TYPE = Symbol.for("react.provider");
        var REACT_CONTEXT_TYPE = Symbol.for("react.context");
        var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
        var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
        var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
        var REACT_MEMO_TYPE = Symbol.for("react.memo");
        var REACT_LAZY_TYPE = Symbol.for("react.lazy");
        var REACT_OFFSCREEN_TYPE = Symbol.for("react.offscreen");
        var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
        var FAUX_ITERATOR_SYMBOL = "@@iterator";
        function getIteratorFn(maybeIterable) {
          if (maybeIterable === null || typeof maybeIterable !== "object") {
            return null;
          }
          var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
          if (typeof maybeIterator === "function") {
            return maybeIterator;
          }
          return null;
        }
        var ReactSharedInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
        function error(format) {
          {
            {
              for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
                args[_key2 - 1] = arguments[_key2];
              }
              printWarning("error", format, args);
            }
          }
        }
        function printWarning(level, format, args) {
          {
            var ReactDebugCurrentFrame2 = ReactSharedInternals.ReactDebugCurrentFrame;
            var stack = ReactDebugCurrentFrame2.getStackAddendum();
            if (stack !== "") {
              format += "%s";
              args = args.concat([stack]);
            }
            var argsWithFormat = args.map(function(item) {
              return String(item);
            });
            argsWithFormat.unshift("Warning: " + format);
            Function.prototype.apply.call(console[level], console, argsWithFormat);
          }
        }
        var enableScopeAPI = false;
        var enableCacheElement = false;
        var enableTransitionTracing = false;
        var enableLegacyHidden = false;
        var enableDebugTracing = false;
        var REACT_MODULE_REFERENCE;
        {
          REACT_MODULE_REFERENCE = Symbol.for("react.module.reference");
        }
        function isValidElementType(type) {
          if (typeof type === "string" || typeof type === "function") {
            return true;
          }
          if (type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || enableDebugTracing || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || enableLegacyHidden || type === REACT_OFFSCREEN_TYPE || enableScopeAPI || enableCacheElement || enableTransitionTracing) {
            return true;
          }
          if (typeof type === "object" && type !== null) {
            if (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || // This needs to include all possible module reference object
            // types supported by any Flight configuration anywhere since
            // we don't know which Flight build this will end up being used
            // with.
            type.$$typeof === REACT_MODULE_REFERENCE || type.getModuleId !== void 0) {
              return true;
            }
          }
          return false;
        }
        function getWrappedName(outerType, innerType, wrapperName) {
          var displayName = outerType.displayName;
          if (displayName) {
            return displayName;
          }
          var functionName = innerType.displayName || innerType.name || "";
          return functionName !== "" ? wrapperName + "(" + functionName + ")" : wrapperName;
        }
        function getContextName(type) {
          return type.displayName || "Context";
        }
        function getComponentNameFromType(type) {
          if (type == null) {
            return null;
          }
          {
            if (typeof type.tag === "number") {
              error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue.");
            }
          }
          if (typeof type === "function") {
            return type.displayName || type.name || null;
          }
          if (typeof type === "string") {
            return type;
          }
          switch (type) {
            case REACT_FRAGMENT_TYPE:
              return "Fragment";
            case REACT_PORTAL_TYPE:
              return "Portal";
            case REACT_PROFILER_TYPE:
              return "Profiler";
            case REACT_STRICT_MODE_TYPE:
              return "StrictMode";
            case REACT_SUSPENSE_TYPE:
              return "Suspense";
            case REACT_SUSPENSE_LIST_TYPE:
              return "SuspenseList";
          }
          if (typeof type === "object") {
            switch (type.$$typeof) {
              case REACT_CONTEXT_TYPE:
                var context = type;
                return getContextName(context) + ".Consumer";
              case REACT_PROVIDER_TYPE:
                var provider = type;
                return getContextName(provider._context) + ".Provider";
              case REACT_FORWARD_REF_TYPE:
                return getWrappedName(type, type.render, "ForwardRef");
              case REACT_MEMO_TYPE:
                var outerName = type.displayName || null;
                if (outerName !== null) {
                  return outerName;
                }
                return getComponentNameFromType(type.type) || "Memo";
              case REACT_LAZY_TYPE: {
                var lazyComponent = type;
                var payload = lazyComponent._payload;
                var init = lazyComponent._init;
                try {
                  return getComponentNameFromType(init(payload));
                } catch (x) {
                  return null;
                }
              }
            }
          }
          return null;
        }
        var assign = Object.assign;
        var disabledDepth = 0;
        var prevLog;
        var prevInfo;
        var prevWarn;
        var prevError;
        var prevGroup;
        var prevGroupCollapsed;
        var prevGroupEnd;
        function disabledLog() {
        }
        disabledLog.__reactDisabledLog = true;
        function disableLogs() {
          {
            if (disabledDepth === 0) {
              prevLog = console.log;
              prevInfo = console.info;
              prevWarn = console.warn;
              prevError = console.error;
              prevGroup = console.group;
              prevGroupCollapsed = console.groupCollapsed;
              prevGroupEnd = console.groupEnd;
              var props = {
                configurable: true,
                enumerable: true,
                value: disabledLog,
                writable: true
              };
              Object.defineProperties(console, {
                info: props,
                log: props,
                warn: props,
                error: props,
                group: props,
                groupCollapsed: props,
                groupEnd: props
              });
            }
            disabledDepth++;
          }
        }
        function reenableLogs() {
          {
            disabledDepth--;
            if (disabledDepth === 0) {
              var props = {
                configurable: true,
                enumerable: true,
                writable: true
              };
              Object.defineProperties(console, {
                log: assign({}, props, {
                  value: prevLog
                }),
                info: assign({}, props, {
                  value: prevInfo
                }),
                warn: assign({}, props, {
                  value: prevWarn
                }),
                error: assign({}, props, {
                  value: prevError
                }),
                group: assign({}, props, {
                  value: prevGroup
                }),
                groupCollapsed: assign({}, props, {
                  value: prevGroupCollapsed
                }),
                groupEnd: assign({}, props, {
                  value: prevGroupEnd
                })
              });
            }
            if (disabledDepth < 0) {
              error("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
            }
          }
        }
        var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
        var prefix;
        function describeBuiltInComponentFrame(name, source, ownerFn) {
          {
            if (prefix === void 0) {
              try {
                throw Error();
              } catch (x) {
                var match = x.stack.trim().match(/\n( *(at )?)/);
                prefix = match && match[1] || "";
              }
            }
            return "\n" + prefix + name;
          }
        }
        var reentry = false;
        var componentFrameCache;
        {
          var PossiblyWeakMap = typeof WeakMap === "function" ? WeakMap : Map;
          componentFrameCache = new PossiblyWeakMap();
        }
        function describeNativeComponentFrame(fn, construct) {
          if (!fn || reentry) {
            return "";
          }
          {
            var frame = componentFrameCache.get(fn);
            if (frame !== void 0) {
              return frame;
            }
          }
          var control;
          reentry = true;
          var previousPrepareStackTrace = Error.prepareStackTrace;
          Error.prepareStackTrace = void 0;
          var previousDispatcher;
          {
            previousDispatcher = ReactCurrentDispatcher.current;
            ReactCurrentDispatcher.current = null;
            disableLogs();
          }
          try {
            if (construct) {
              var Fake = function() {
                throw Error();
              };
              Object.defineProperty(Fake.prototype, "props", {
                set: function() {
                  throw Error();
                }
              });
              if (typeof Reflect === "object" && Reflect.construct) {
                try {
                  Reflect.construct(Fake, []);
                } catch (x) {
                  control = x;
                }
                Reflect.construct(fn, [], Fake);
              } else {
                try {
                  Fake.call();
                } catch (x) {
                  control = x;
                }
                fn.call(Fake.prototype);
              }
            } else {
              try {
                throw Error();
              } catch (x) {
                control = x;
              }
              fn();
            }
          } catch (sample) {
            if (sample && control && typeof sample.stack === "string") {
              var sampleLines = sample.stack.split("\n");
              var controlLines = control.stack.split("\n");
              var s = sampleLines.length - 1;
              var c = controlLines.length - 1;
              while (s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c]) {
                c--;
              }
              for (; s >= 1 && c >= 0; s--, c--) {
                if (sampleLines[s] !== controlLines[c]) {
                  if (s !== 1 || c !== 1) {
                    do {
                      s--;
                      c--;
                      if (c < 0 || sampleLines[s] !== controlLines[c]) {
                        var _frame = "\n" + sampleLines[s].replace(" at new ", " at ");
                        if (fn.displayName && _frame.includes("<anonymous>")) {
                          _frame = _frame.replace("<anonymous>", fn.displayName);
                        }
                        {
                          if (typeof fn === "function") {
                            componentFrameCache.set(fn, _frame);
                          }
                        }
                        return _frame;
                      }
                    } while (s >= 1 && c >= 0);
                  }
                  break;
                }
              }
            }
          } finally {
            reentry = false;
            {
              ReactCurrentDispatcher.current = previousDispatcher;
              reenableLogs();
            }
            Error.prepareStackTrace = previousPrepareStackTrace;
          }
          var name = fn ? fn.displayName || fn.name : "";
          var syntheticFrame = name ? describeBuiltInComponentFrame(name) : "";
          {
            if (typeof fn === "function") {
              componentFrameCache.set(fn, syntheticFrame);
            }
          }
          return syntheticFrame;
        }
        function describeFunctionComponentFrame(fn, source, ownerFn) {
          {
            return describeNativeComponentFrame(fn, false);
          }
        }
        function shouldConstruct(Component) {
          var prototype = Component.prototype;
          return !!(prototype && prototype.isReactComponent);
        }
        function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
          if (type == null) {
            return "";
          }
          if (typeof type === "function") {
            {
              return describeNativeComponentFrame(type, shouldConstruct(type));
            }
          }
          if (typeof type === "string") {
            return describeBuiltInComponentFrame(type);
          }
          switch (type) {
            case REACT_SUSPENSE_TYPE:
              return describeBuiltInComponentFrame("Suspense");
            case REACT_SUSPENSE_LIST_TYPE:
              return describeBuiltInComponentFrame("SuspenseList");
          }
          if (typeof type === "object") {
            switch (type.$$typeof) {
              case REACT_FORWARD_REF_TYPE:
                return describeFunctionComponentFrame(type.render);
              case REACT_MEMO_TYPE:
                return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
              case REACT_LAZY_TYPE: {
                var lazyComponent = type;
                var payload = lazyComponent._payload;
                var init = lazyComponent._init;
                try {
                  return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
                } catch (x) {
                }
              }
            }
          }
          return "";
        }
        var hasOwnProperty = Object.prototype.hasOwnProperty;
        var loggedTypeFailures = {};
        var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
        function setCurrentlyValidatingElement(element) {
          {
            if (element) {
              var owner = element._owner;
              var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
              ReactDebugCurrentFrame.setExtraStackFrame(stack);
            } else {
              ReactDebugCurrentFrame.setExtraStackFrame(null);
            }
          }
        }
        function checkPropTypes(typeSpecs, values, location, componentName, element) {
          {
            var has = Function.call.bind(hasOwnProperty);
            for (var typeSpecName in typeSpecs) {
              if (has(typeSpecs, typeSpecName)) {
                var error$1 = void 0;
                try {
                  if (typeof typeSpecs[typeSpecName] !== "function") {
                    var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                    err.name = "Invariant Violation";
                    throw err;
                  }
                  error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
                } catch (ex) {
                  error$1 = ex;
                }
                if (error$1 && !(error$1 instanceof Error)) {
                  setCurrentlyValidatingElement(element);
                  error("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || "React class", location, typeSpecName, typeof error$1);
                  setCurrentlyValidatingElement(null);
                }
                if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
                  loggedTypeFailures[error$1.message] = true;
                  setCurrentlyValidatingElement(element);
                  error("Failed %s type: %s", location, error$1.message);
                  setCurrentlyValidatingElement(null);
                }
              }
            }
          }
        }
        var isArrayImpl = Array.isArray;
        function isArray(a2) {
          return isArrayImpl(a2);
        }
        function typeName(value) {
          {
            var hasToStringTag = typeof Symbol === "function" && Symbol.toStringTag;
            var type = hasToStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
            return type;
          }
        }
        function willCoercionThrow(value) {
          {
            try {
              testStringCoercion(value);
              return false;
            } catch (e) {
              return true;
            }
          }
        }
        function testStringCoercion(value) {
          return "" + value;
        }
        function checkKeyStringCoercion(value) {
          {
            if (willCoercionThrow(value)) {
              error("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
              return testStringCoercion(value);
            }
          }
        }
        var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner;
        var RESERVED_PROPS = {
          key: true,
          ref: true,
          __self: true,
          __source: true
        };
        var specialPropKeyWarningShown;
        var specialPropRefWarningShown;
        var didWarnAboutStringRefs;
        {
          didWarnAboutStringRefs = {};
        }
        function hasValidRef(config) {
          {
            if (hasOwnProperty.call(config, "ref")) {
              var getter = Object.getOwnPropertyDescriptor(config, "ref").get;
              if (getter && getter.isReactWarning) {
                return false;
              }
            }
          }
          return config.ref !== void 0;
        }
        function hasValidKey(config) {
          {
            if (hasOwnProperty.call(config, "key")) {
              var getter = Object.getOwnPropertyDescriptor(config, "key").get;
              if (getter && getter.isReactWarning) {
                return false;
              }
            }
          }
          return config.key !== void 0;
        }
        function warnIfStringRefCannotBeAutoConverted(config, self) {
          {
            if (typeof config.ref === "string" && ReactCurrentOwner.current && self && ReactCurrentOwner.current.stateNode !== self) {
              var componentName = getComponentNameFromType(ReactCurrentOwner.current.type);
              if (!didWarnAboutStringRefs[componentName]) {
                error('Component "%s" contains the string ref "%s". Support for string refs will be removed in a future major release. This case cannot be automatically converted to an arrow function. We ask you to manually fix this case by using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref', getComponentNameFromType(ReactCurrentOwner.current.type), config.ref);
                didWarnAboutStringRefs[componentName] = true;
              }
            }
          }
        }
        function defineKeyPropWarningGetter(props, displayName) {
          {
            var warnAboutAccessingKey = function() {
              if (!specialPropKeyWarningShown) {
                specialPropKeyWarningShown = true;
                error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
              }
            };
            warnAboutAccessingKey.isReactWarning = true;
            Object.defineProperty(props, "key", {
              get: warnAboutAccessingKey,
              configurable: true
            });
          }
        }
        function defineRefPropWarningGetter(props, displayName) {
          {
            var warnAboutAccessingRef = function() {
              if (!specialPropRefWarningShown) {
                specialPropRefWarningShown = true;
                error("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
              }
            };
            warnAboutAccessingRef.isReactWarning = true;
            Object.defineProperty(props, "ref", {
              get: warnAboutAccessingRef,
              configurable: true
            });
          }
        }
        var ReactElement = function(type, key, ref, self, source, owner, props) {
          var element = {
            // This tag allows us to uniquely identify this as a React Element
            $$typeof: REACT_ELEMENT_TYPE,
            // Built-in properties that belong on the element
            type,
            key,
            ref,
            props,
            // Record the component responsible for creating this element.
            _owner: owner
          };
          {
            element._store = {};
            Object.defineProperty(element._store, "validated", {
              configurable: false,
              enumerable: false,
              writable: true,
              value: false
            });
            Object.defineProperty(element, "_self", {
              configurable: false,
              enumerable: false,
              writable: false,
              value: self
            });
            Object.defineProperty(element, "_source", {
              configurable: false,
              enumerable: false,
              writable: false,
              value: source
            });
            if (Object.freeze) {
              Object.freeze(element.props);
              Object.freeze(element);
            }
          }
          return element;
        };
        function jsxDEV(type, config, maybeKey, source, self) {
          {
            var propName;
            var props = {};
            var key = null;
            var ref = null;
            if (maybeKey !== void 0) {
              {
                checkKeyStringCoercion(maybeKey);
              }
              key = "" + maybeKey;
            }
            if (hasValidKey(config)) {
              {
                checkKeyStringCoercion(config.key);
              }
              key = "" + config.key;
            }
            if (hasValidRef(config)) {
              ref = config.ref;
              warnIfStringRefCannotBeAutoConverted(config, self);
            }
            for (propName in config) {
              if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
                props[propName] = config[propName];
              }
            }
            if (type && type.defaultProps) {
              var defaultProps = type.defaultProps;
              for (propName in defaultProps) {
                if (props[propName] === void 0) {
                  props[propName] = defaultProps[propName];
                }
              }
            }
            if (key || ref) {
              var displayName = typeof type === "function" ? type.displayName || type.name || "Unknown" : type;
              if (key) {
                defineKeyPropWarningGetter(props, displayName);
              }
              if (ref) {
                defineRefPropWarningGetter(props, displayName);
              }
            }
            return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
          }
        }
        var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner;
        var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
        function setCurrentlyValidatingElement$1(element) {
          {
            if (element) {
              var owner = element._owner;
              var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
              ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
            } else {
              ReactDebugCurrentFrame$1.setExtraStackFrame(null);
            }
          }
        }
        var propTypesMisspellWarningShown;
        {
          propTypesMisspellWarningShown = false;
        }
        function isValidElement(object) {
          {
            return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
          }
        }
        function getDeclarationErrorAddendum() {
          {
            if (ReactCurrentOwner$1.current) {
              var name = getComponentNameFromType(ReactCurrentOwner$1.current.type);
              if (name) {
                return "\n\nCheck the render method of `" + name + "`.";
              }
            }
            return "";
          }
        }
        function getSourceInfoErrorAddendum(source) {
          {
            if (source !== void 0) {
              var fileName = source.fileName.replace(/^.*[\\\/]/, "");
              var lineNumber = source.lineNumber;
              return "\n\nCheck your code at " + fileName + ":" + lineNumber + ".";
            }
            return "";
          }
        }
        var ownerHasKeyUseWarning = {};
        function getCurrentComponentErrorInfo(parentType) {
          {
            var info = getDeclarationErrorAddendum();
            if (!info) {
              var parentName = typeof parentType === "string" ? parentType : parentType.displayName || parentType.name;
              if (parentName) {
                info = "\n\nCheck the top-level render call using <" + parentName + ">.";
              }
            }
            return info;
          }
        }
        function validateExplicitKey(element, parentType) {
          {
            if (!element._store || element._store.validated || element.key != null) {
              return;
            }
            element._store.validated = true;
            var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
            if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
              return;
            }
            ownerHasKeyUseWarning[currentComponentErrorInfo] = true;
            var childOwner = "";
            if (element && element._owner && element._owner !== ReactCurrentOwner$1.current) {
              childOwner = " It was passed a child from " + getComponentNameFromType(element._owner.type) + ".";
            }
            setCurrentlyValidatingElement$1(element);
            error('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', currentComponentErrorInfo, childOwner);
            setCurrentlyValidatingElement$1(null);
          }
        }
        function validateChildKeys(node, parentType) {
          {
            if (typeof node !== "object") {
              return;
            }
            if (isArray(node)) {
              for (var i = 0; i < node.length; i++) {
                var child = node[i];
                if (isValidElement(child)) {
                  validateExplicitKey(child, parentType);
                }
              }
            } else if (isValidElement(node)) {
              if (node._store) {
                node._store.validated = true;
              }
            } else if (node) {
              var iteratorFn = getIteratorFn(node);
              if (typeof iteratorFn === "function") {
                if (iteratorFn !== node.entries) {
                  var iterator = iteratorFn.call(node);
                  var step;
                  while (!(step = iterator.next()).done) {
                    if (isValidElement(step.value)) {
                      validateExplicitKey(step.value, parentType);
                    }
                  }
                }
              }
            }
          }
        }
        function validatePropTypes(element) {
          {
            var type = element.type;
            if (type === null || type === void 0 || typeof type === "string") {
              return;
            }
            var propTypes;
            if (typeof type === "function") {
              propTypes = type.propTypes;
            } else if (typeof type === "object" && (type.$$typeof === REACT_FORWARD_REF_TYPE || // Note: Memo only checks outer props here.
            // Inner props are checked in the reconciler.
            type.$$typeof === REACT_MEMO_TYPE)) {
              propTypes = type.propTypes;
            } else {
              return;
            }
            if (propTypes) {
              var name = getComponentNameFromType(type);
              checkPropTypes(propTypes, element.props, "prop", name, element);
            } else if (type.PropTypes !== void 0 && !propTypesMisspellWarningShown) {
              propTypesMisspellWarningShown = true;
              var _name = getComponentNameFromType(type);
              error("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", _name || "Unknown");
            }
            if (typeof type.getDefaultProps === "function" && !type.getDefaultProps.isReactClassApproved) {
              error("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
            }
          }
        }
        function validateFragmentProps(fragment) {
          {
            var keys = Object.keys(fragment.props);
            for (var i = 0; i < keys.length; i++) {
              var key = keys[i];
              if (key !== "children" && key !== "key") {
                setCurrentlyValidatingElement$1(fragment);
                error("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", key);
                setCurrentlyValidatingElement$1(null);
                break;
              }
            }
            if (fragment.ref !== null) {
              setCurrentlyValidatingElement$1(fragment);
              error("Invalid attribute `ref` supplied to `React.Fragment`.");
              setCurrentlyValidatingElement$1(null);
            }
          }
        }
        function jsxWithValidation(type, props, key, isStaticChildren, source, self) {
          {
            var validType = isValidElementType(type);
            if (!validType) {
              var info = "";
              if (type === void 0 || typeof type === "object" && type !== null && Object.keys(type).length === 0) {
                info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
              }
              var sourceInfo = getSourceInfoErrorAddendum(source);
              if (sourceInfo) {
                info += sourceInfo;
              } else {
                info += getDeclarationErrorAddendum();
              }
              var typeString;
              if (type === null) {
                typeString = "null";
              } else if (isArray(type)) {
                typeString = "array";
              } else if (type !== void 0 && type.$$typeof === REACT_ELEMENT_TYPE) {
                typeString = "<" + (getComponentNameFromType(type.type) || "Unknown") + " />";
                info = " Did you accidentally export a JSX literal instead of a component?";
              } else {
                typeString = typeof type;
              }
              error("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", typeString, info);
            }
            var element = jsxDEV(type, props, key, source, self);
            if (element == null) {
              return element;
            }
            if (validType) {
              var children = props.children;
              if (children !== void 0) {
                if (isStaticChildren) {
                  if (isArray(children)) {
                    for (var i = 0; i < children.length; i++) {
                      validateChildKeys(children[i], type);
                    }
                    if (Object.freeze) {
                      Object.freeze(children);
                    }
                  } else {
                    error("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
                  }
                } else {
                  validateChildKeys(children, type);
                }
              }
            }
            if (type === REACT_FRAGMENT_TYPE) {
              validateFragmentProps(element);
            } else {
              validatePropTypes(element);
            }
            return element;
          }
        }
        function jsxWithValidationStatic(type, props, key) {
          {
            return jsxWithValidation(type, props, key, true);
          }
        }
        function jsxWithValidationDynamic(type, props, key) {
          {
            return jsxWithValidation(type, props, key, false);
          }
        }
        var jsx = jsxWithValidationDynamic;
        var jsxs = jsxWithValidationStatic;
        exports.Fragment = REACT_FRAGMENT_TYPE;
        exports.jsx = jsx;
        exports.jsxs = jsxs;
      })();
    }
  }
});

// node_modules/react/jsx-runtime.js
var require_jsx_runtime = __commonJS({
  "node_modules/react/jsx-runtime.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_react_jsx_runtime_development();
    }
  }
});

// node_modules/mantine-datatable/dist/index.mjs
var d = __toESM(require_react(), 1);
var import_react = __toESM(require_react(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_react3 = __toESM(require_react(), 1);
var import_react4 = __toESM(require_react(), 1);
var import_react5 = __toESM(require_react(), 1);
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var import_react6 = __toESM(require_react(), 1);
var import_react7 = __toESM(require_react(), 1);
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
var import_react8 = __toESM(require_react(), 1);
var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
var import_react9 = __toESM(require_react(), 1);
var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime20 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime21 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime22 = __toESM(require_jsx_runtime(), 1);


var a = Ba(() => {
  "use strict";
});
var Ue = Qo((Nf, We) => {
  "use strict";
  a();
  var wa = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
  We.exports = wa;
});
var Xe = Qo((Wf, Ge) => {
  "use strict";
  a();


  };
});
var Ke = Qo((Gf, Qe) => {
  a();
  Qe.exports = Xe()();
  var zf, Vf;
});
a();
a();
a();
var Pa = createStyles({ root: { "&&": { background: "transparent", ":last-of-type td": { borderBottom: "none" } } } });
function Ko() {
  let { classes: o } = Pa();
  return (0, import_jsx_runtime.jsx)("tr", { className: o.root, children: (0, import_jsx_runtime.jsx)("td", {}) });
}
a();
a();
a();
var N = ya(Ke());


};
var q = (o, e, r) => {
  let t = (0, import_react2.forwardRef)((f, n) => {
    var l = f, { color: s = "currentColor", size: u = 24, stroke: m = 2, children: p } = l, C = Aa(l, ["color", "size", "stroke", "children"]);
    return (0, import_react2.createElement)("svg", _e(Oa(_e({ ref: n }, Ze), { width: u, height: u, stroke: s, strokeWidth: m, className: `tabler-icon tabler-icon-${o}` }), C), [...r.map(([c, I]) => (0, import_react2.createElement)(c, I)), ...p || []]);
  });
  return t.propTypes = { color: N.default.string, size: N.default.oneOfType([N.default.string, N.default.number]), stroke: N.default.oneOfType([N.default.string, N.default.number]) }, t.displayName = `${e}`, t;
};
a();
var Zo = q("arrow-up", "IconArrowUp", [["path", { d: "M12 5l0 14", key: "svg-0" }], ["path", { d: "M18 11l-6 -6", key: "svg-1" }], ["path", { d: "M6 11l6 -6", key: "svg-2" }]]);
a();
var $o = q("arrows-vertical", "IconArrowsVertical", [["path", { d: "M8 7l4 -4l4 4", key: "svg-0" }], ["path", { d: "M8 17l4 4l4 -4", key: "svg-1" }], ["path", { d: "M12 3l0 18", key: "svg-2" }]]);
a();
var Yo = q("database-off", "IconDatabaseOff", [["path", { d: "M12.983 8.978c3.955 -.182 7.017 -1.446 7.017 -2.978c0 -1.657 -3.582 -3 -8 -3c-1.661 0 -3.204 .19 -4.483 .515m-2.783 1.228c-.471 .382 -.734 .808 -.734 1.257c0 1.22 1.944 2.271 4.734 2.74", key: "svg-0" }], ["path", { d: "M4 6v6c0 1.657 3.582 3 8 3c.986 0 1.93 -.067 2.802 -.19m3.187 -.82c1.251 -.53 2.011 -1.228 2.011 -1.99v-6", key: "svg-1" }], ["path", { d: "M4 12v6c0 1.657 3.582 3 8 3c3.217 0 5.991 -.712 7.261 -1.74m.739 -3.26v-4", key: "svg-2" }], ["path", { d: "M3 3l18 18", key: "svg-3" }]]);
a();
var _o = q("filter", "IconFilter", [["path", { d: "M4 4h16v2.172a2 2 0 0 1 -.586 1.414l-4.414 4.414v7l-6 2v-8.5l-4.48 -4.928a2 2 0 0 1 -.52 -1.345v-2.227z", key: "svg-0" }]]);
var Na = createStyles((o) => ({ root: { position: "absolute", top: 0, right: 0, bottom: 0, left: 0, flexDirection: "column", pointerEvents: "none", color: o.colorScheme === "dark" ? o.colors.dark[3] : o.colors.gray[6], opacity: 0, transition: "opacity .15s ease" }, active: { opacity: 1 }, standardIcon: { fontSize: 0, borderRadius: "50%", padding: o.spacing.xs, background: o.colorScheme === "dark" ? o.colors.dark[5] : o.colors.gray[2] } }));
function jo({ icon: o, text: e, pt: r, pb: t, active: f, children: n }) {
  let { classes: l, cx: s } = Na();
  return (0, import_jsx_runtime2.jsx)(Center, { pt: r, pb: t, className: s(l.root, { [l.active]: f }), children: n || (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [o || (0, import_jsx_runtime2.jsx)("div", { className: l.standardIcon, children: (0, import_jsx_runtime2.jsx)(Yo, {}) }), (0, import_jsx_runtime2.jsx)(Text, { size: "sm", color: "dimmed", children: e })] }) });
}
a();
a();
a();
a();
function Ga(o, e) {
  let r = () => {
    e(o.map((f) => f.matches));
  }, t = o.map((f) => {
    try {
      return f.addEventListener("change", r), () => f.removeEventListener("change", r);
    } catch {
      return f.addListener(r), () => f.removeListener(r);
    }
  });
  return () => {
    t.forEach((f) => f());
  };
}
function Xa(o, e) {
  return e || (typeof window < "u" && "matchMedia" in window ? o.map((r) => window.matchMedia(r).matches) : o.map(() => false));
}
function or(o, e, { getInitialValueInEffect: r } = { getInitialValueInEffect: true }) {
  let [t, f] = (0, import_react5.useState)(r ? e : Xa(o, e)), n = (0, import_react5.useRef)();
  return (0, import_react5.useEffect)(() => {
    if ("matchMedia" in window)
      return n.current = o.map((l) => window.matchMedia(l)), f(n.current.map((l) => l.matches)), Ga(n.current, (l) => {
        f(l);
      });
  }, [o]), t;
}
var ar = typeof window < "u" ? import_react4.useLayoutEffect : import_react4.useEffect;
function Y(o) {
  let e = useMantineTheme(), r = typeof o == "function" ? o(e) : o;
  return useMediaQuery(r || "", true);
}
function tr(o) {
  let e = useMantineTheme(), r = (0, import_react4.useMemo)(() => o.map((f) => (typeof f == "function" ? f(e) : f) ?? ""), [o, e]), t = (0, import_react4.useMemo)(() => o.map(() => true), [o]);
  return or(r, t);
}
function Q(o) {
  let e = o.replace(/([a-z\d])([A-Z]+)/g, "$1 $2").replace(/\W|_/g, " ").trim().toLowerCase();
  return `${e.charAt(0).toUpperCase()}${e.slice(1)}`;
}
function Jo(o, e, r) {
  return o.filter((t) => !e.map(r).includes(r(t)));
}
function co(o, e) {
  return o.filter((r, t, f) => t === f.findIndex((n) => e(r) === e(n)));
}
function wo(o, e) {
  return e ? e.match(/([^[.\]])+/g).reduce((t, f) => t && t[f], o) : void 0;
}
function y(o, e) {
  return typeof e == "string" ? wo(o, e) : e(o);
}
var _a = createStyles({ noWrap: { whiteSpace: "nowrap" }, ellipsis: { overflow: "hidden", textOverflow: "ellipsis" } });
function oe({ className: o, sx: e, style: r, visibleMediaQuery: t, title: f, noWrap: n, ellipsis: l, textAlignment: s, width: u }) {
  let { cx: m, classes: p } = _a();
  return Y(t) ? (0, import_jsx_runtime3.jsx)(Box, { component: "th", className: m({ [p.noWrap]: n || l, [p.ellipsis]: l }, o), sx: [{ "&&": { textAlign: s }, width: u, minWidth: u, maxWidth: u }, e], style: r, children: f }) : null;
}
a();
var et = createStyles((o) => {
  let e = o.colorScheme === "dark" ? 0.5 : 0.05;
  return { root: { position: "sticky", width: 0, left: 0, background: "inherit", "&::after": { content: '""', position: "absolute", top: 0, right: -px(o.spacing.sm), bottom: 0, borderLeft: `1px solid ${o.colorScheme === "dark" ? o.colors.dark[4] : o.colors.gray[3]}`, width: o.spacing.sm, background: `linear-gradient(to right, ${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)}), linear-gradient(to right, ${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)} 30%)`, pointerEvents: "none", opacity: 0, transition: "opacity .15s ease" } }, shadowVisible: { "&::after": { opacity: 1 } } };
});
function ee({ shadowVisible: o }) {
  let { cx: e, classes: r } = et();
  return (0, import_jsx_runtime4.jsx)("th", { className: e(r.root, { [r.shadowVisible]: o }) });
}
var nt = createStyles((o, { scrollDiff: e, borderColor: r }) => {
  let t = e < 0, f = typeof r == "function" ? r(o) : r;
  return { root: { zIndex: 2, position: t ? "relative" : "sticky", bottom: t ? e : -1, background: o.colorScheme === "dark" ? o.colors.dark[7] : o.white, "&& tr th": { borderTopColor: f } }, relative: { position: "relative" }, textSelectionDisabled: { userSelect: "none" } };
});
var fr = (0, import_react3.forwardRef)(function({ className: e, style: r, borderColor: t, columns: f, defaultColumnProps: n, selectionVisible: l, leftShadowVisible: s, scrollDiff: u }, m) {
  let { cx: p, classes: C } = nt({ scrollDiff: u, borderColor: t });
  return (0, import_jsx_runtime5.jsx)(Box, { component: "tfoot", ref: m, className: p(C.root, e), style: r, children: (0, import_jsx_runtime5.jsxs)("tr", { children: [l && (0, import_jsx_runtime5.jsx)(ee, { shadowVisible: s }), f.map(({ hidden: c, ...I }) => {
    if (c)
      return null;
    let { accessor: T, visibleMediaQuery: i, textAlignment: h, width: g, footer: D, footerClassName: S, footerStyle: H, footerSx: R, noWrap: P, ellipsis: F } = { ...n, ...I };
    return (0, import_jsx_runtime5.jsx)(oe, { className: S, style: H, sx: R, visibleMediaQuery: i, textAlignment: h, width: g, title: D, noWrap: P, ellipsis: F }, T);
  })] }) });
});
a();
a();
function ae({ group: { id: o, columns: e, title: r, className: t, style: f, sx: n } }) {
  let l = (0, import_react7.useMemo)(() => e.map(({ visibleMediaQuery: m }) => m), [e]), s = tr(l), u = (0, import_react7.useMemo)(() => e.filter(({ hidden: m }, p) => !m && (s == null ? void 0 : s[p])).length, [e, s]);
  return u > 0 ? (0, import_jsx_runtime6.jsx)(Box, { component: "th", colSpan: u, className: t, sx: n, style: f, children: r ?? Q(o) }) : null;
}
a();
a();
function fe({ children: o, isActive: e }) {
  let [r, { close: t, toggle: f }] = useDisclosure(false);
  return (0, import_jsx_runtime7.jsxs)(Popover, { withArrow: true, withinPortal: true, shadow: "md", opened: r, onClose: t, trapFocus: true, children: [(0, import_jsx_runtime7.jsx)(Popover.Target, { children: (0, import_jsx_runtime7.jsx)(ActionIcon, { onClick: (n) => {
    n.preventDefault(), f();
  }, variant: e ? "default" : "subtle", children: (0, import_jsx_runtime7.jsx)(_o, { size: 14 }) }) }), (0, import_jsx_runtime7.jsx)(Popover.Dropdown, { onClick: (n) => n.preventDefault(), children: typeof o == "function" ? o({ close: t }) : o })] });
}
var It = createStyles((o) => ({ sortableColumnHeader: { cursor: "pointer", transition: "background .15s ease", "&:hover:not(:has(button:hover))": { background: o.colorScheme === "dark" ? o.colors.dark[6] : o.colors.gray[0] } }, sortableColumnHeaderGroup: { gap: "0.25em" }, columnHeaderText: { overflow: "hidden", whiteSpace: "nowrap", textOverflow: "ellipsis" }, sortableColumnHeaderText: { minWidth: 0, flexGrow: 1 }, sortableColumnHeaderIcon: { transition: "transform .15s ease" }, sortableColumnHeaderIconRotated: { transform: "rotate3d(0, 0, 1, 180deg)" }, sortableColumnHeaderUnsortedIcon: { color: o.colorScheme === "dark" ? o.colors.dark[3] : o.colors.gray[5], transition: "color .15s ease", "th:hover &": { color: o.colorScheme === "dark" ? o.colors.dark[2] : o.colors.gray[6] } } }));
function ne({ className: o, sx: e, style: r, accessor: t, visibleMediaQuery: f, title: n, sortable: l, sortIcons: s, textAlignment: u, width: m, sortStatus: p, onSortStatusChange: C, filter: c, filtering: I }) {
  let { cx: T, classes: i } = It();
  if (!Y(f))
    return null;
  let h = n ?? Q(t), g = typeof h == "string" ? h : void 0, D = l && C ? (S) => {
    (S == null ? void 0 : S.defaultPrevented) || C({ columnAccessor: t, direction: (p == null ? void 0 : p.columnAccessor) === t ? p.direction === "asc" ? "desc" : "asc" : (p == null ? void 0 : p.direction) ?? "asc" });
  } : void 0;
  return (0, import_jsx_runtime8.jsx)(Box, { component: "th", className: T({ [i.sortableColumnHeader]: l }, o), sx: [{ "&&": { textAlign: u }, width: m, minWidth: m, maxWidth: m }, e], style: r, role: l ? "button" : void 0, tabIndex: l ? 0 : void 0, onClick: D, onKeyDown: (S) => S.key === "Enter" && (D == null ? void 0 : D()), children: (0, import_jsx_runtime8.jsxs)(Group, { className: i.sortableColumnHeaderGroup, position: "apart", noWrap: true, children: [(0, import_jsx_runtime8.jsx)(Box, { className: T(i.columnHeaderText, i.sortableColumnHeaderText), title: g, children: h }), l || (p == null ? void 0 : p.columnAccessor) === t ? (0, import_jsx_runtime8.jsx)(import_jsx_runtime8.Fragment, { children: (p == null ? void 0 : p.columnAccessor) === t ? (0, import_jsx_runtime8.jsx)(Center, { className: T(i.sortableColumnHeaderIcon, { [i.sortableColumnHeaderIconRotated]: p.direction === "desc" }), role: "img", "aria-label": `Sorted ${p.direction === "desc" ? "descending" : "ascending"}`, children: (s == null ? void 0 : s.sorted) || (0, import_jsx_runtime8.jsx)(Zo, { size: 14 }) }) : (0, import_jsx_runtime8.jsx)(Center, { className: i.sortableColumnHeaderUnsortedIcon, role: "img", "aria-label": "Not sorted", children: (s == null ? void 0 : s.unsorted) || (0, import_jsx_runtime8.jsx)($o, { size: 14 }) }) }) : null, c ? (0, import_jsx_runtime8.jsx)(fe, { isActive: !!I, children: c }) : null] }) });
}
a();
var bt = createStyles((o) => {
  let e = o.colorScheme === "dark" ? 0.5 : 0.05;
  return { root: { position: "sticky", width: 0, left: 0, background: "inherit", "&::after": { content: '""', position: "absolute", top: 0, right: -px(o.spacing.sm), bottom: 0, borderLeft: `1px solid ${o.colorScheme === "dark" ? o.colors.dark[4] : o.colors.gray[3]}`, width: o.spacing.sm, background: `linear-gradient(to right, ${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)}), linear-gradient(to right, ${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)} 30%)`, pointerEvents: "none", opacity: 0, transition: "opacity .15s ease" } }, shadowVisible: { "&::after": { opacity: 1 } }, checkboxInput: { cursor: "pointer" } };
});
function le({ shadowVisible: o, checked: e, indeterminate: r, checkboxProps: t, onChange: f, rowSpan: n }) {
  let { cx: l, classes: s } = bt();
  return (0, import_jsx_runtime9.jsx)("th", { className: l(s.root, { [s.shadowVisible]: o }), rowSpan: n, children: (0, import_jsx_runtime9.jsx)(Checkbox, { classNames: { input: s.checkboxInput }, checked: e, indeterminate: r, disabled: !f, onChange: f, ...t }) });
}
var yt = createStyles((o) => ({ root: { zIndex: 2, position: "sticky", top: 0, background: o.colorScheme === "dark" ? o.colors.dark[7] : o.white }, textSelectionDisabled: { userSelect: "none" } }));
var cr = (0, import_react6.forwardRef)(function({ className: e, style: r, sortStatus: t, sortIcons: f, onSortStatusChange: n, columns: l, defaultColumnProps: s, groups: u, selectionVisible: m, selectionChecked: p, selectionIndeterminate: C, onSelectionChange: c, selectionCheckboxProps: I, leftShadowVisible: T }, i) {
  let { classes: h, cx: g } = yt(), D = m ? (0, import_jsx_runtime10.jsx)(le, { shadowVisible: T, checked: p, indeterminate: C, checkboxProps: I, onChange: c, rowSpan: u ? 2 : void 0 }) : null;
  return (0, import_jsx_runtime10.jsxs)("thead", { className: g(h.root, e), style: r, ref: i, children: [u && (0, import_jsx_runtime10.jsxs)("tr", { children: [D, u.map((S) => (0, import_jsx_runtime10.jsx)(ae, { group: S }, S.id))] }), (0, import_jsx_runtime10.jsxs)("tr", { children: [!u && D, l.map(({ hidden: S, ...H }) => {
    if (S)
      return null;
    let { accessor: R, visibleMediaQuery: P, textAlignment: F, width: O, title: v, sortable: A, titleClassName: K, titleStyle: j, titleSx: J, filter: oo, filtering: eo } = { ...s, ...H };
    return (0, import_jsx_runtime10.jsx)(ne, { accessor: R, className: K, style: j, sx: J, visibleMediaQuery: P, textAlignment: F, width: O, title: v, sortable: A, sortStatus: t, sortIcons: f, onSortStatusChange: n, filter: oo, filtering: eo }, R);
  })] })] });
});
a();
var Ft = createStyles((o) => ({ root: { zIndex: 3, position: "absolute", top: 0, left: 0, right: 0, bottom: 0, pointerEvents: "none", background: o.fn.rgba(o.colorScheme === "dark" ? o.colors.dark[8] : o.white, 0.75), opacity: 0, transition: "opacity .15s ease" }, fetching: { pointerEvents: "all", opacity: 1 } }));
function ce({ pt: o, pb: e, fetching: r, customContent: t, backgroundBlur: f, size: n, variant: l, color: s }) {
  let { classes: u, cx: m } = Ft();
  return (0, import_jsx_runtime11.jsx)(Center, { pt: o, pb: e, className: m(u.root, { [u.fetching]: r }), sx: f ? { backdropFilter: `blur(${f}px)` } : void 0, children: r && (t || (0, import_jsx_runtime11.jsx)(Loader, { size: n, variant: l, color: s })) });
}
a();
a();
var mr = { xs: 22, sm: 26, md: 32, lg: 38, xl: 44 };
function ue({ size: o, label: e, values: r, value: t, onChange: f, color: n }) {
  return (0, import_jsx_runtime12.jsxs)(Group, { spacing: "xs", children: [(0, import_jsx_runtime12.jsx)(Text, { size: o, children: e }), (0, import_jsx_runtime12.jsxs)(Menu, { withinPortal: true, withArrow: true, children: [(0, import_jsx_runtime12.jsx)(Menu.Target, { children: (0, import_jsx_runtime12.jsx)(Button, { size: o, variant: "default", sx: [{ fontWeight: "normal" }, (l) => ({ height: mr[o], paddingLeft: l.spacing[o], paddingRight: l.spacing[o] })], children: t }) }), (0, import_jsx_runtime12.jsx)(Menu.Dropdown, { children: r.map((l) => {
    let s = l === t;
    return (0, import_jsx_runtime12.jsx)(Menu.Item, { sx: [{ height: mr[o] }, (u) => ({ "&&": { color: s ? u.white : void 0 }, background: s ? u.colors[n || u.primaryColor][6] : void 0 })], disabled: s, onClick: () => f(l), children: (0, import_jsx_runtime12.jsx)(Text, { size: o, children: l }) }, l);
  }) })] })] });
}
var Ht = createStyles((o, { topBorderColor: e, paginationWrapBreakpoint: r }) => ({ root: { background: o.colorScheme === "dark" ? o.colors.dark[7] : o.white, borderTop: `1px solid ${typeof e == "function" ? e(o) : e}`, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "space-between", gap: o.spacing.xs, [o.fn.largerThan(r)]: { flexDirection: "row" } }, text: { flex: "1 1 auto" }, pagination: { opacity: 1, transition: "opacity .15s ease" }, paginationFetching: { opacity: 0 } }));
var Ir = (0, import_react8.forwardRef)(function({ className: e, style: r, topBorderColor: t, fetching: f, page: n, onPageChange: l, paginationColor: s, paginationSize: u, loadingText: m, noRecordsText: p, paginationText: C, totalRecords: c, recordsPerPage: I, onRecordsPerPageChange: T, recordsPerPageLabel: i, recordsPerPageOptions: h, recordsLength: g, horizontalSpacing: D, paginationWrapBreakpoint: S, getPaginationControlProps: H }, R) {
  let P;
  if (f)
    P = m;
  else if (!c)
    P = p;
  else {
    let v = (n - 1) * I + 1, A = v + g - 1;
    P = C({ from: v, to: A, totalRecords: c });
  }
  let { classes: F, cx: O } = Ht({ topBorderColor: t, paginationWrapBreakpoint: S });
  return (0, import_jsx_runtime13.jsxs)(Box, { ref: R, px: D ?? "xs", py: "xs", className: O(F.root, e), style: r, children: [(0, import_jsx_runtime13.jsx)(Text, { className: F.text, size: u, children: P }), h && (0, import_jsx_runtime13.jsx)(ue, { size: u, label: i, values: h, color: s, value: I, onChange: T }), (0, import_jsx_runtime13.jsx)(Pagination, { color: s, className: O(F.pagination, { [F.paginationFetching]: f || !g }), value: n, onChange: l, size: u, total: Math.ceil(c / I), getControlProps: H })] });
});
a();
a();
var Wt = createStyles({ withPointerCursor: { cursor: "pointer" }, noWrap: { whiteSpace: "nowrap" }, ellipsis: { overflow: "hidden", textOverflow: "ellipsis" } });
function me({ className: o, sx: e, style: r, visibleMediaQuery: t, record: f, recordIndex: n, onClick: l, noWrap: s, ellipsis: u, textAlignment: m, width: p, accessor: C, render: c, defaultRender: I, customCellAttributes: T }) {
  let { cx: i, classes: h } = Wt();
  return Y(t) ? (0, import_jsx_runtime14.jsx)(Box, { component: "td", className: i({ [h.noWrap]: s || u, [h.ellipsis]: u, [h.withPointerCursor]: l }, o), sx: [{ width: p, minWidth: p, maxWidth: p, textAlign: m }, e], style: r, onClick: l, ...T == null ? void 0 : T(f, n), children: c ? c(f, n) : I ? I(f, n, C) : wo(f, C) }) : null;
}
a();
a();
function gr(o) {
  let [e, r] = (0, import_react9.useState)(null), t = (o == null ? void 0 : o.join(":")) || "";
  return (0, import_react9.useEffect)(() => {
    r(null);
  }, [t]), { lastSelectionChangeIndex: e, setLastSelectionChangeIndex: r };
}
function Cr(o) {
  let [e, r] = (0, import_react9.useState)(null);
  return (0, import_react9.useEffect)(() => {
    o && r(null);
  }, [o]), { rowContextMenuInfo: e, setRowContextMenuInfo: r };
}
function hr({ rowExpansion: o, records: e, idAccessor: r }) {
  let t = [];
  if (o && e) {
    let { trigger: s, allowMultiple: u, initiallyExpanded: m } = o;
    e && s === "always" ? t = e.map((p) => y(p, r)) : m && (t = e.filter(m).map((p) => y(p, r)), u || (t = [t[0]]));
  }
  let f, n, l = (0, import_react9.useState)(t);
  if (o) {
    let { trigger: s, allowMultiple: u, collapseProps: m, content: p } = o;
    o.expanded ? { recordIds: f, onRecordIdsChange: n } = o.expanded : [f, n] = l;
    let C = (c) => n == null ? void 0 : n(f.filter((I) => I !== y(c, r)));
    return { expandOnClick: s !== "always" && s !== "never", isRowExpanded: (c) => s === "always" ? true : f.includes(y(c, r)), expandRow: (c) => {
      let I = y(c, r);
      n == null ? void 0 : n(u ? [...f, I] : [I]);
    }, collapseRow: C, collapseProps: m, content: (c, I) => () => p({ record: c, recordIndex: I, collapse: () => C(c) }) };
  }
}
function Sr(o, e) {
  let [r, t] = (0, import_react9.useState)(o), [f, n] = (0, import_react9.useState)(o), l = useTimeout(() => t(true), 0), s = useTimeout(() => n(false), e || 200);
  return (0, import_react9.useEffect)(() => {
    o ? (s.clear(), n(true), l.start()) : (l.clear(), t(false), s.start());
  }, [l, s, o]), { expanded: r, visible: f };
}
var Gt = createStyles({ cell: { "&&": { borderBottomWidth: 0, padding: 0 } }, expandedCell: { "&&": { borderBottomWidth: 1 } } });
function Ie({ open: o, colSpan: e, content: r, collapseProps: t }) {
  let { expanded: f, visible: n } = Sr(o, t == null ? void 0 : t.transitionDuration), { cx: l, classes: s } = Gt();
  return n ? (0, import_jsx_runtime15.jsxs)(import_jsx_runtime15.Fragment, { children: [(0, import_jsx_runtime15.jsx)("tr", {}), (0, import_jsx_runtime15.jsx)("tr", { children: (0, import_jsx_runtime15.jsx)("td", { className: l(s.cell, { [s.expandedCell]: f }), colSpan: e, children: (0, import_jsx_runtime15.jsx)(Collapse, { in: f, ...t, children: r() }) }) })] }) : null;
}
a();
var Yt = createStyles((o) => {
  let e = o.colorScheme === "dark" ? 0.5 : 0.05;
  return { root: { position: "sticky", zIndex: 1, width: 0, left: 0, background: "inherit", "&::after": { content: '""', position: "absolute", top: 0, right: -px(o.spacing.sm), bottom: 0, borderLeft: `1px solid ${o.colorScheme === "dark" ? o.colors.dark[4] : o.colors.gray[3]}`, width: o.spacing.sm, background: `linear-gradient(to right, ${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)}), linear-gradient(to right, ${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)} 30%)`, pointerEvents: "none", opacity: 0, transition: "opacity .15s ease" } }, withRightShadow: { "&::after": { opacity: 1 } }, checkbox: { cursor: "pointer" } };
});
function ie({ record: o, recordIndex: e, withRightShadow: r, getCheckboxProps: t, ...f }) {
  let { cx: n, classes: l } = Yt();
  return (0, import_jsx_runtime16.jsx)("td", { className: n(l.root, { [l.withRightShadow]: r }), onClick: (s) => s.stopPropagation(), children: (0, import_jsx_runtime16.jsx)(Checkbox, { classNames: { input: l.checkbox }, ...f, ...t(o, e) }) });
}
var Jt = createStyles((o) => {
  let e = o.colors[o.primaryColor][6];
  return { withPointerCursor: { cursor: "pointer" }, selected: { "&&": { "tr&": { background: o.colorScheme === "dark" ? o.fn.darken(e, 0.6) : o.fn.lighten(e, 0.9) }, "table[data-striped] tbody &:nth-of-type(odd)": { background: o.colorScheme === "dark" ? o.fn.darken(e, 0.55) : o.fn.lighten(e, 0.85) } } }, contextMenuVisible: { "&&": { "tr&": { background: o.colorScheme === "dark" ? o.fn.darken(e, 0.5) : o.fn.lighten(e, 0.7) }, "table[data-striped] tbody &:nth-of-type(odd)": { background: o.colorScheme === "dark" ? o.fn.darken(e, 0.45) : o.fn.lighten(e, 0.65) } } } };
});
function Ce({ record: o, recordIndex: e, columns: r, defaultColumnProps: t, defaultColumnRender: f, selectionVisible: n, selectionChecked: l, onSelectionChange: s, isRecordSelectable: u, getSelectionCheckboxProps: m, onClick: p, onCellClick: C, onContextMenu: c, expansion: I, customAttributes: T, className: i, style: h, sx: g, contextMenuVisible: D, leftShadowVisible: S }) {
  let { cx: H, classes: R } = Jt();
  return (0, import_jsx_runtime17.jsxs)(import_jsx_runtime17.Fragment, { children: [(0, import_jsx_runtime17.jsxs)(Box, { component: "tr", className: H({ [R.withPointerCursor]: p || (I == null ? void 0 : I.expandOnClick), [R.selected]: l, [R.contextMenuVisible]: D }, typeof i == "function" ? i(o, e) : i), onClick: (P) => {
    if (I) {
      let { isRowExpanded: F, expandOnClick: O, expandRow: v, collapseRow: A } = I;
      O && (F(o) ? A(o) : v(o));
    }
    p == null ? void 0 : p(P);
  }, style: typeof h == "function" ? h(o, e) : h, sx: g, ...T == null ? void 0 : T(o, e), onContextMenu: c, children: [n && (0, import_jsx_runtime17.jsx)(ie, { record: o, recordIndex: e, withRightShadow: S, checked: l, disabled: !s || (u ? !u(o, e) : false), onChange: s, getCheckboxProps: m }), r.map(({ hidden: P, ...F }, O) => {
    if (P)
      return null;
    let { accessor: v, visibleMediaQuery: A, textAlignment: K, noWrap: j, ellipsis: J, width: oo, render: eo, cellsClassName: ro, cellsStyle: ao, cellsSx: Oo, customCellAttributes: Ao } = { ...t, ...F }, mo;
    return C && (mo = (Lo) => C({ event: Lo, record: o, recordIndex: e, column: F, columnIndex: O })), (0, import_jsx_runtime17.jsx)(me, { className: typeof ro == "function" ? ro(o, e) : ro, style: typeof ao == "function" ? ao(o, e) : ao, sx: Oo, visibleMediaQuery: A, record: o, recordIndex: e, onClick: mo, accessor: v, textAlignment: K, noWrap: j, ellipsis: J, width: oo, render: eo, defaultRender: f, customCellAttributes: Ao }, v);
  })] }), I && (0, import_jsx_runtime17.jsx)(Ie, { colSpan: r.filter(({ hidden: P }) => !P).length + (n ? 1 : 0), open: I.isRowExpanded(o), content: I.content(o, e), collapseProps: I.collapseProps })] });
}
a();
var lf = createStyles((o) => ({ root: { position: "fixed", border: `1px solid ${o.colorScheme === "dark" ? o.colors.dark[4] : o.colors.gray[3]}`, overflow: "hidden", transition: "all .15s ease" } }));
function he({ borderRadius: o = "xs", shadow: e = "sm", zIndex: r = 3, y: t, x: f, onDestroy: n, children: l }) {
  useWindowEvent("resize", n), useWindowEvent("scroll", n);
  let s = useClickOutside(n), { ref: u, width: m, height: p } = useElementSize(), C = useMergedRef(s, u), { innerWidth: c, innerHeight: I } = window, { classes: T, theme: { dir: i, spacing: h } } = lf(), g = px(h.md);
  return (0, import_jsx_runtime18.jsx)(Paper, { ref: C, shadow: e, radius: o, className: T.root, sx: { zIndex: r, top: t + p + g > I ? I - p - g : t, left: i === "ltr" ? f + m + g > c ? c - m - g : f : c - g - (f - m - g < 0 ? m + g : f) }, children: l });
}
a();
var cf = createStyles((o) => ({ root: { height: 1, background: o.colorScheme === "dark" ? o.colors.dark[5] : o.colors.gray[1] } }));
function Se() {
  let { classes: o } = cf();
  return (0, import_jsx_runtime19.jsx)("div", { className: o.root });
}
a();
var Cf = createStyles((o, { color: e }) => {
  let r = px(o.spacing.sm) / 2;
  return { root: { width: "100%", display: "flex", alignItems: "center", paddingTop: r, paddingBottom: r, paddingLeft: o.spacing.sm, paddingRight: o.spacing.sm, color: e && o.colors[e][6], transition: "background .15s ease", "&[disabled]": { cursor: "not-allowed", color: o.colorScheme === "dark" ? o.colors.dark[3] : o.colors.gray[5] }, "&:hover:not([disabled])": { background: o.fn.rgba(e ? o.colors[e][6] : o.colorScheme === "dark" ? o.colors.dark[3] : o.colors.gray[4], e ? o.colorScheme === "dark" ? 0.15 : 0.08 : 0.25) }, "&:active:not([disabled])": { background: o.fn.rgba(e ? o.colors[e][6] : o.colorScheme === "dark" ? o.colors.dark[3] : o.colors.gray[4], e ? o.colorScheme === "dark" ? 0.3 : 0.2 : 0.5) } }, icon: { fontSize: 0, marginRight: o.spacing.xs }, title: { whiteSpace: "nowrap" } };
});
function be({ icon: o, title: e, color: r, disabled: t, onClick: f }) {
  let { classes: n } = Cf({ color: r });
  return (0, import_jsx_runtime20.jsxs)(UnstyledButton, { className: n.root, disabled: t, onClick: f, children: [o && (0, import_jsx_runtime20.jsx)(Box, { className: n.icon, children: o }), (0, import_jsx_runtime20.jsx)(Text, { className: n.title, size: "sm", children: e })] });
}
a();
var Bf = createStyles((o) => {
  let e = o.colorScheme === "dark" ? 0.5 : 0.05;
  return { root: { flex: "1 1 100%" }, scrollbar: { '&[data-state="visible"]': { background: "transparent" }, "div::before": { pointerEvents: "none" } }, corner: { background: "transparent" }, thumb: { zIndex: 3 }, shadow: { position: "absolute", pointerEvents: "none", opacity: 0, transition: "opacity .15s ease" }, topShadow: { zIndex: 2, left: 0, right: 0, height: o.spacing.sm, background: `linear-gradient(${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)}), linear-gradient(${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)} 30%)` }, leftShadow: { zIndex: 3, top: 0, left: 0, bottom: 0, width: o.spacing.sm, background: `linear-gradient(to right, ${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)}), linear-gradient(to right, ${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)} 30%)` }, rightShadow: { zIndex: 2, top: 0, bottom: 0, right: 0, width: o.spacing.sm, background: `linear-gradient(to left, ${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)}), linear-gradient(to left, ${o.fn.rgba(o.black, e)}, ${o.fn.rgba(o.black, 0)} 30%)` }, bottomShadow: { zIndex: 2, left: 0, right: 0, height: o.spacing.sm, background: `linear-gradient(${o.fn.rgba(o.black, 0)}, ${o.fn.rgba(o.black, e)}), linear-gradient(${o.fn.rgba(o.black, 0)} 30%, ${o.fn.rgba(o.black, e)})` }, shadowVisible: { opacity: 1 } };
});
function Be({ topShadowVisible: o, leftShadowVisible: e, rightShadowVisible: r, bottomShadowVisible: t, headerHeight: f, footerHeight: n, onScrollPositionChange: l, children: s, viewportRef: u, scrollAreaProps: m }) {
  let p = n ? n - 1 : 0, { cx: C, classes: c } = Bf();
  return (0, import_jsx_runtime21.jsxs)(ScrollArea, { ...m, viewportRef: u, classNames: { root: c.root, scrollbar: c.scrollbar, thumb: c.thumb, corner: c.corner }, styles: { scrollbar: { marginTop: f, marginBottom: p } }, onScrollPositionChange: l, children: [s, (0, import_jsx_runtime21.jsx)(Box, { className: C(c.shadow, c.topShadow, { [c.shadowVisible]: o }), sx: { top: f } }), (0, import_jsx_runtime21.jsx)("div", { className: C(c.shadow, c.leftShadow, { [c.shadowVisible]: e }) }), (0, import_jsx_runtime21.jsx)("div", { className: C(c.shadow, c.rightShadow, { [c.shadowVisible]: r }) }), (0, import_jsx_runtime21.jsx)(Box, { className: C(c.shadow, c.bottomShadow, { [c.shadowVisible]: t }), sx: { bottom: p } })] });
}
var Pr = {};
var Rf = createStyles((o, { borderColor: e, rowBorderColor: r }) => {
  let t = typeof e == "function" ? e(o) : e, f = typeof r == "function" ? r(o) : r;
  return { root: { position: "relative", display: "flex", flexDirection: "column", overflow: "hidden", tr: { backgroundColor: o.colorScheme === "dark" ? o.colors.dark[7] : o.white }, "&&": { "thead tr th": { borderBottomColor: t }, "tbody tr td": { borderTopColor: f } } }, lastRowBorderBottomVisible: { "tbody tr:last-of-type td": { borderBottom: `1px solid ${f}` } }, textSelectionDisabled: { userSelect: "none" }, table: { borderCollapse: "separate", borderSpacing: 0 }, tableWithBorder: { border: `1px solid ${t}` }, tableWithColumnBorders: { "&&": { "th, td": { ":not(:first-of-type)": { borderLeft: `1px solid ${f}` } } } }, tableWithColumnBordersAndSelectableRecords: { thead: { "tr + tr": { th: { borderLeft: `1px solid ${f}` } } } }, verticalAlignmentTop: { td: { verticalAlign: "top" } }, verticalAlignmentBottom: { td: { verticalAlign: "bottom" } } };
});
function Fr({ withBorder: o, borderRadius: e, borderColor: r = (B) => B.colorScheme === "dark" ? B.colors.dark[4] : B.colors.gray[3], rowBorderColor: t = (B) => B.fn.rgba(B.colorScheme === "dark" ? B.colors.dark[4] : B.colors.gray[3], 0.65), withColumnBorders: f, textSelectionDisabled: n, height: l = "100%", minHeight: s, shadow: u, verticalAlignment: m = "center", fetching: p, columns: C, groups: c, defaultColumnProps: I, defaultColumnRender: T, idAccessor: i = "id", records: h, selectedRecords: g, onSelectedRecordsChange: D, isRecordSelectable: S, allRecordsSelectionCheckboxProps: H = { "aria-label": "Select all records" }, getRecordSelectionCheckboxProps: R = (B, U) => ({ "aria-label": `Select record ${U + 1}` }), sortStatus: P, sortIcons: F, onSortStatusChange: O, horizontalSpacing: v, page: A, onPageChange: K, totalRecords: j, recordsPerPage: J, onRecordsPerPageChange: oo, recordsPerPageOptions: eo, recordsPerPageLabel: ro = "Records per page", paginationColor: ao, paginationSize: Oo = "sm", paginationText: Ao = ({ from: B, to: U, totalRecords: z }) => `${B} - ${U} / ${z}`, paginationWrapBreakpoint: mo = "sm", getPaginationControlProps: Lo = (B) => B === "previous" ? { "aria-label": "Previous page" } : B === "next" ? { "aria-label": "Next page" } : {}, loaderBackgroundBlur: Mr, customLoader: kr, loaderSize: Rr, loaderVariant: vr, loaderColor: Or, loadingText: Ar = "...", emptyState: Lr, noRecordsText: ye = "No records", noRecordsIcon: Hr, striped: Er, noHeader: Nr, onRowClick: Ho, onCellClick: qr, onScrollToTop: Te, onScrollToBottom: Pe, onScrollToLeft: we, onScrollToRight: Fe, rowContextMenu: L, rowExpansion: Wr, rowClassName: Ur, rowStyle: zr, rowSx: Vr, customRowAttributes: Gr, scrollViewportRef: Xr, scrollAreaProps: Qr, bodyRef: Kr, m: Zr, my: $r, mx: Yr, mt: _r, mb: jr, ml: Jr, mr: oa, sx: ea, className: ra, classNames: xo, style: aa, styles: Eo, ...ta }) {
  let { ref: B, width: U, height: z } = useElementSize(), Io = (0, import_react.useMemo)(() => (c == null ? void 0 : c.flatMap((x) => x.columns)) ?? C, [C, c]), { ref: fa, height: No } = useElementSize(), { ref: na, width: qo, height: to } = useElementSize(), { ref: la, height: sa } = useElementSize(), { ref: da, height: Me } = useElementSize(), [Wo, ke] = (0, import_react.useState)(true), [Uo, Re] = (0, import_react.useState)(true), [io, ve] = (0, import_react.useState)(true), [zo, Oe] = (0, import_react.useState)(true), { rowContextMenuInfo: V, setRowContextMenuInfo: Z } = Cr(p), ca = hr({ rowExpansion: Wr, records: h, idAccessor: i }), Vo = (0, import_react.useCallback)(() => {
    if (!p && L && Z(null), p || to <= z)
      ke(true), Re(true);
    else {
      let x = B.current.scrollTop, b = x === 0, M = to - x - z < 1;
      ke(b), Re(M), b && b !== Wo && (Te == null ? void 0 : Te()), M && M !== Uo && (Pe == null ? void 0 : Pe());
    }
    if (p || qo === U)
      ve(true), Oe(true);
    else {
      let x = B.current.scrollLeft, b = x === 0, M = qo - x - U < 1;
      ve(b), Oe(M), b && b !== io && (we == null ? void 0 : we()), M && M !== zo && (Fe == null ? void 0 : Fe());
    }
  }, [p, Pe, we, Fe, Te, L, z, B, U, Uo, io, zo, Wo, Z, to, qo]);
  ar(Vo, [Vo]);
  let ua = (0, import_react.useCallback)((x) => {
    B.current.scrollTo({ top: 0, left: 0 }), K(x);
  }, [K, B]), go = h == null ? void 0 : h.length, Ae = h == null ? void 0 : h.map((x) => y(x, i)), fo = !!g, no = g == null ? void 0 : g.map((x) => y(x, i)), Le = Ae !== void 0 && no !== void 0 && no.length > 0, Go = S ? h == null ? void 0 : h.filter(S) : h, Co = Go == null ? void 0 : Go.map((x) => y(x, i)), ho = Le && Co.every((x) => no.includes(x)), pa = Le && Co.some((x) => no.includes(x)), ma = (0, import_react.useCallback)(() => {
    g && D && D(ho ? g.filter((x) => !Co.includes(y(x, i))) : co([...g, ...Go], (x) => y(x, i)));
  }, [ho, i, D, Co, Go, g]), { lastSelectionChangeIndex: So, setLastSelectionChangeIndex: xa } = gr(Ae), Xo = fo && !io, { cx: He, classes: E, theme: Ia } = Rf({ borderColor: r, rowBorderColor: t }), ia = { m: Zr, my: $r, mx: Yr, mt: _r, mb: jr, ml: Jr, mr: oa }, bo = typeof Eo == "function" ? Eo(Ia, Pr, Pr) : Eo;
  return (0, import_jsx_runtime22.jsxs)(Box, { ...ia, className: He(E.root, { [E.tableWithBorder]: o }, ra, xo == null ? void 0 : xo.root), sx: [(x) => ({ borderRadius: x.radius[e] || e, boxShadow: x.shadows[u] || u, height: l, minHeight: s }), ...packSx(ea)], style: { ...bo == null ? void 0 : bo.root, ...aa }, children: [(0, import_jsx_runtime22.jsx)(Be, { viewportRef: useMergedRef(B, Xr || null), topShadowVisible: !Wo, leftShadowVisible: !(g || io), rightShadowVisible: !zo, bottomShadowVisible: !Uo, headerHeight: No, footerHeight: sa, onScrollPositionChange: Vo, scrollAreaProps: Qr, children: (0, import_jsx_runtime22.jsxs)(Table, { ref: na, horizontalSpacing: v, className: He(E.table, { [E.tableWithColumnBorders]: f, [E.lastRowBorderBottomVisible]: to < z, [E.textSelectionDisabled]: n, [E.verticalAlignmentTop]: m === "top", [E.verticalAlignmentBottom]: m === "bottom", [E.tableWithColumnBordersAndSelectableRecords]: fo && f }), striped: go ? Er : false, ...ta, children: [Nr ? null : (0, import_jsx_runtime22.jsx)(cr, { ref: fa, className: xo == null ? void 0 : xo.header, style: bo == null ? void 0 : bo.header, columns: Io, defaultColumnProps: I, groups: c, sortStatus: P, sortIcons: F, onSortStatusChange: O, selectionVisible: fo, selectionChecked: ho, selectionIndeterminate: pa && !ho, onSelectionChange: ma, selectionCheckboxProps: H, leftShadowVisible: Xo }), (0, import_jsx_runtime22.jsx)("tbody", { ref: Kr, children: go ? h.map((x, b) => {
    let M = y(x, i), lo = (no == null ? void 0 : no.includes(M)) || false, Bo = false, Do = false;
    if (L) {
      let { hidden: w } = L;
      (!w || !(typeof w == "function" ? w(x, b) : w)) && (L.trigger === "click" ? Bo = true : Do = true);
    }
    let yo;
    D && g && (yo = (w) => {
      if (w.nativeEvent.shiftKey && So !== null) {
        let $ = h.filter(b > So ? (G, X) => X >= So && X <= b && (S ? S(G, X) : true) : (G, X) => X >= b && X <= So && (S ? S(G, X) : true));
        D(lo ? Jo(g, $, (G) => y(G, i)) : co([...g, ...$], (G) => y(G, i)));
      } else
        D(lo ? g.filter(($) => y($, i) !== M) : co([...g, x], ($) => y($, i)));
      xa(b);
    });
    let so;
    Bo ? so = (w) => {
      Z({ y: w.clientY, x: w.clientX, record: x, recordIndex: b }), Ho == null ? void 0 : Ho(x, b, w);
    } : Ho && (so = (w) => {
      Ho(x, b, w);
    });
    let Ee;
    return Do && (Ee = (w) => {
      w.preventDefault(), Z({ y: w.clientY, x: w.clientX, record: x, recordIndex: b });
    }), (0, import_jsx_runtime22.jsx)(Ce, { record: x, recordIndex: b, columns: Io, defaultColumnProps: I, defaultColumnRender: T, selectionVisible: fo, selectionChecked: lo, onSelectionChange: yo, isRecordSelectable: S, getSelectionCheckboxProps: R, onClick: so, onCellClick: qr, onContextMenu: Ee, contextMenuVisible: V ? y(V.record, i) === M : false, expansion: ca, className: Ur, style: zr, sx: Vr, customAttributes: Gr, leftShadowVisible: Xo }, M);
  }) : (0, import_jsx_runtime22.jsx)(Ko, {}) }), Io.some(({ footer: x }) => x) && (0, import_jsx_runtime22.jsx)(fr, { ref: la, className: xo == null ? void 0 : xo.footer, style: bo == null ? void 0 : bo.footer, borderColor: r, columns: Io, defaultColumnProps: I, selectionVisible: fo, leftShadowVisible: Xo, scrollDiff: to - z })] }) }), A && (0, import_jsx_runtime22.jsx)(Ir, { ref: da, className: xo == null ? void 0 : xo.pagination, style: bo == null ? void 0 : bo.pagination, topBorderColor: r, horizontalSpacing: v, fetching: p, page: A, onPageChange: ua, totalRecords: j, recordsPerPage: J, onRecordsPerPageChange: oo, recordsPerPageOptions: eo, recordsPerPageLabel: ro, paginationColor: ao, paginationSize: Oo, paginationText: Ao, paginationWrapBreakpoint: mo, getPaginationControlProps: Lo, noRecordsText: ye, loadingText: Ar, recordsLength: go }), (0, import_jsx_runtime22.jsx)(ce, { pt: No, pb: Me, fetching: p, backgroundBlur: Mr, customContent: kr, size: Rr, variant: vr, color: Or }), (0, import_jsx_runtime22.jsx)(jo, { pt: No, pb: Me, icon: Hr, text: ye, active: !p && !go, children: Lr }), L && V && (0, import_jsx_runtime22.jsx)(Portal, { children: (0, import_jsx_runtime22.jsx)(he, { zIndex: L.zIndex, borderRadius: L.borderRadius, shadow: L.shadow, y: V.y, x: V.x, onDestroy: () => Z(null), children: L.items(V.record, V.recordIndex).map(({ divider: x, key: b, title: M, icon: lo, color: Bo, hidden: Do, disabled: yo, onClick: so }) => x ? (0, import_jsx_runtime22.jsx)(Se, {}, b) : Do ? null : (0, import_jsx_runtime22.jsx)(be, { title: M ?? Q(b), icon: lo, color: Bo, disabled: yo, onClick: () => {
    Z(null), so();
  } }, b)) }) })] });
}
a();
a();
a();
a();
a();
a();
a();
a();
a();
a();
a();
a();
a();
a();
a();
a();
a();
a();
export {
  Fr as DataTable,
  Jo as differenceBy,
  wo as getValueAtPath,
  Q as humanize,
  co as uniqBy
};
/*! Bundled license information:

react/cjs/react-jsx-runtime.development.js:
  (**
   * @license React
   * react-jsx-runtime.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
//# sourceMappingURL=mantine-datatable.js.map
